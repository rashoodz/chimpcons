---
title: Field manual
description: Durable, local-first operations tools for consultants.
icon: BookOpen
---

import {
  Blocks,
  Database,
  Files,
  Globe,
  Presentation,
  ScanSearch,
  Scissors,
  SplitSquareVertical,
  TableProperties,
} from "lucide-react";

ConsultChimps turns recurring document and data chores into deterministic tools.
Use the [online tools](/tools) when a file is already in front of you, the
command-line interface when you need an answer now, or the focused TypeScript
packages when you are building a repeatable workflow. The browser tools are
built from the same operation code as the CLI and libraries; a few defaults
differ by design and are called out in each tool's guide.

<Callout title="Built for client work">
  Inputs are immutable by default, outputs include provenance, and the core
  operations do not require a hosted service.
</Callout>

## Start with a tool

<Cards>
  <Card
    title="Use the tools online"
    description="Work with Excel, PDF, and PowerPoint files, or import workbooks into a persistent local database, in your browser"
    href="/tools"
    icon={<Globe />}
  />
  <Card
    title="Consolidate spreadsheets"
    description="Combine visible, non-empty worksheets into one auditable Excel table"
    href="/docs/tools/spreadsheet-consolidate"
    icon={<TableProperties />}
  />
  <Card
    title="Merge workbook tabs"
    description="Copy every source worksheet into one workbook without flattening the tabs"
    href="/docs/tools/workbook-merge"
    icon={<Files />}
  />
  <Card
    title="Split a spreadsheet"
    description="Create one Excel workbook for each distinct value in a selected column"
    href="/docs/tools/spreadsheet-split"
    icon={<SplitSquareVertical />}
  />
  <Card
    title="Inspect a workbook"
    description="Report the worksheets, header rows, Excel Tables, and named ranges a workbook holds"
    href="/docs/tools/workbook-inspect"
    icon={<ScanSearch />}
  />
  <Card
    title="Populate a PowerPoint template"
    description="Create one formatted slide for every nonempty Excel record"
    href="/docs/tools/powerpoint-populate"
    icon={<Presentation />}
  />
  <Card
    title="Split a PDF"
    description="Write every page to a predictably named PDF"
    href="/docs/tools/pdf-split"
    icon={<Scissors />}
  />
  <Card
    title="Merge PDFs"
    description="Assemble PDFs in resolved input order"
    href="/docs/tools/pdf-merge"
    icon={<Files />}
  />
  <Card
    title="Import workbooks into a database"
    description="Create SQLite or DuckDB files, review imports, and record delivery history"
    href="/docs/tools/data-workspace"
    icon={<Database />}
  />
  <Card
    title="Compose with libraries"
    description="Import the file, table, workbook, PowerPoint, and PDF modules directly"
    href="/docs/libraries"
    icon={<Blocks />}
  />
</Cards>

## The operating model

Every operation returns the same result shape:

```ts
interface OperationResult {
  operation: string;
  artifacts: Array<{
    kind: "file" | "directory";
    path: string;
    mediaType?: string;
  }>;
  warnings: string[];
  metrics: Record<string, number>;
}
```

Normal terminal output turns that result into a detailed explanation for a
non-technical reader. It explains what happened, translates counts into plain
language, lists every created file, calls out warnings, confirms source-file
safety, and suggests what to do next. Automations can use `--json` to receive
the stable result shape directly.

## Design principles

- **Local-first:** client files remain on the machine running the operation.
- **Immutable source documents:** database writes are explicit operations;
  imports leave source workbooks unchanged.
- **Composable modules:** each capability is useful without the CLI.
- **Explicit provenance:** consolidated rows can retain file, sheet, and row.
- **Plain-language guidance:** every CLI tool explains its results and next
  steps without assuming technical knowledge.
- **Replaceable intelligence:** future OCR or AI providers remain optional.
